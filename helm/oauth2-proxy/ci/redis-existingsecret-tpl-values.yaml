# Test: tpl support for sessionStorage.redis.existingSecret
# Verifies that a Helm template expression in existingSecret is rendered correctly.
sessionStorage:
  type: redis
  redis:
    clientType: standalone
    existingSecret: "{{ .Release.Name }}-redis-secret"
    passwordKey: redis-password
    standalone:
      connectionUrl: "redis://redis:6379"
